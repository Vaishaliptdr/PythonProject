# Package is folder containing one or more modules
# We need to crate package in directory
# In package file named "__init__.py" present by default
# If we want to convert directory/folder in package then
# simply add "__init__.py" file in directory/folder